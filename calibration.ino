void setup() {
  // put your setup code here, to run once:
  Serial.begin(9600)

}

void loop() {
  // put your main code here, to run repeatedly:
  
}

void calibrate() {
  // Get readings from gyroscope

  // Store readings into a 3 lists
  vector<float> X;
  vector<float> Y;
  vecotr<float> Z;
  // Take the average of all values after 5 seconds and Standard deviation

  // Store averages and std's and return them

  return cal_X, cal_Y, cal_Z, offset_X, offset_Y, offset_Z
}